version: "3.9"

volumes:
  frontend_node_modules:

services:
  frontend:
    build:
      context: ./frontend/
      target: dev
    tty: true
    restart: always
    ports:
      - "3030:3000"
    volumes:
      - ./frontend:/app/frontend
      - frontend_node_modules:/app/frontend/node_modules
